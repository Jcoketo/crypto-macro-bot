name: Daily Crypto Macro Analysis

on:
  schedule:
    - cron: "0 13 * * *"   # 13 UTC ≈ 10 AM Argentina
  workflow_dispatch:

jobs:
  run-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run analysis bot
        run: |
          python bot.py
